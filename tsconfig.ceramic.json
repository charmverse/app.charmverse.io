{
  "extends": "./tsconfig.next.json",
  "exclude": [
    "node_modules",
    "__e2e__"
  ],
  "ts-node": {
    "transpileOnly": true,
    "moduleTypes": {
      "**/*.ts": "esm", // This is in progress. We have a CJS / ESM issue with multiformats library imported by ceramic when running writeComposite & graphQl server. Currently can be overcome by setting package.json type to module. This tsconfig is a search for a better solution that works out of the box.
    }
  },
  "compilerOptions": {
    "module": "ES6"
  }
}