import { AlertBanner } from 'components/common/Banners/Alert';

export function ProposalNotesBanner() {
  return <AlertBanner severity='info'>Reviewer notes are only visible to reviewers</AlertBanner>;
}
